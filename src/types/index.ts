// Re-export database types for backward compatibility
export type {
  Client,
  Document,
  Vendor,
  ChatMessage,
  Task,
  AIInsight,
  PaymentTransaction,
  DocumentRequest,
  DocumentRequestItem,
  EmailCommunication
} from '../lib/database';